import 'package:flutter/widgets.dart';

class FocusPostScreen extends StatelessWidget {
  const FocusPostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
